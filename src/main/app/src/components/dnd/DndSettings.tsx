import * as React from "react"

type PropsSettings = {
    admin:  boolean
    hidden: boolean
}
export default function DndSettings(props: PropsSettings) {
    return (
        <div>

        </div>
    )
}